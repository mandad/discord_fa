[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: hits real network endpoints (SWPC / OVATION / GI / NOAA). Deselect with -m "not live".
filterwarnings =
    ignore::DeprecationWarning
