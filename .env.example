# Required ----------------------------------------------------------------
DISCORD_TOKEN=your-bot-token-here
CHANNEL_ID=000000000000000000          # numeric channel id to post in

# Optional ----------------------------------------------------------------
# GUILD_ID=000000000000000000          # set for instant slash-command sync in one server
# KP_THRESHOLD=4                        # alert when forecast Kp >= this
# MIN_AURORA_PCT=1                      # post hourly only when aurora chance at ship >= this %
# DAILY_POST_UTC=18                     # hour (UTC) of the daily prediction post
# SHIP_NAME=FAIRWEATHER                 # mfphub AIS name match
# SHIP_SCRIPT_PATH=/mnt/c/Users/damia/OneDrive/Documents/LLM_Land/scripts/ship-position.py
# STATE_PATH=state.json
